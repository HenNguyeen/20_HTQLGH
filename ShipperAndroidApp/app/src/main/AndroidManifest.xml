<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="ShipperApp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OrderListActivity" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".OrderMapActivity" android:exported="false" />
        <activity android:name=".MainActivity" />
        
        <!-- Google Maps API Key - Demo/Development Only -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDKj8K_qQ8X5vN5xX5xX5xX5xX5xX5xX5x" />
        <!-- TODO: Replace with your own API key from https://console.cloud.google.com/ -->
    </application>

</manifest>
